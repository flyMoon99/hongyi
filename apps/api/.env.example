# 库须已由超级用户建好；生产只跑 migrate:deploy，勿在服务器上跑 migrate dev（dev 会建 shadow 库，需 CREATEDB 权限）
DATABASE_URL="postgresql://postgres:password@localhost:5432/hongyi_db?schema=public"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"
PORT=3001
# 可选：逗号分隔的前端地址，用于 CORS（默认含 localhost:3000 与 127.0.0.1:3000）
# FRONTEND_ORIGIN="http://localhost:3000,https://your-domain.com"
